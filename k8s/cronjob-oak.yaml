# Weekly knowledge dev-log for the OAK monorepo (hy-oo-app).
# Requires in secret commit-audit-secrets: BITBUCKET_TOKEN (git+API scopes),
# GITLAB_TOKEN with write_repository on the knowledge project.
# The report lands under mds/oak/ and the repo's own CI syncs it to the
# agents' Bedrock knowledge base.
apiVersion: v1
kind: ConfigMap
metadata:
  name: commit-audit-oak-repos
data:
  repos: |
    https://bitbucket.org/luxottica_rona/hy-oo-app.git
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: commit-audit-oak
spec:
  schedule: "30 6 * * 1" # weekly, Monday 06:30 UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 3600 # big clone + LLM; do not let it hang forever
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: audit
              image: europe-west1-docker.pkg.dev/abs-digital-playground/agents/commit-audit:0.4.1
              imagePullPolicy: IfNotPresent
              command: ["sh", "-c"]
              args:
                - |
                  set -e
                  # the default branch of hy-oo-app is quiet: audit whichever
                  # branch was updated most recently (Bitbucket API)
                  BRANCH=$(BITBUCKET_TOKEN="$BITBUCKET_TOKEN" python3 -c "import json,os,urllib.request as u; r=u.Request('https://api.bitbucket.org/2.0/repositories/luxottica_rona/hy-oo-app/refs/branches?sort=-target.date&pagelen=1', headers={'Authorization': 'Bearer ' + os.environ['BITBUCKET_TOKEN']}); print(json.load(u.urlopen(r, timeout=30))['values'][0]['name'])")
                  echo "auditing branch: $BRANCH"
                  CMD="audit --period week --lang italian --branch $BRANCH"
                  while IFS= read -r r; do
                    [ -n "$r" ] && CMD="$CMD --repo $r"
                  done < /etc/audit/repos
                  exec $CMD
              envFrom:
                - secretRef:
                    name: commit-audit-secrets
              env:
                - name: AUDIT_DEPTH
                  value: "3000"
                - name: GITLAB_URL
                  value: "https://gitlab.az.abssrv.it"
                - name: KB_GITLAB_PROJECT
                  value: "innovation/essilux/oo-knowledge"
                - name: KB_PREFIX
                  value: "mds/oak"
                - name: KB_BRANCH
                  value: "main"
              volumeMounts:
                - name: repos
                  mountPath: /etc/audit/repos
                  subPath: repos
                  readOnly: true
              resources:
                requests: { cpu: 100m, memory: 256Mi }
                limits: { memory: 1Gi }
          volumes:
            - name: repos
              configMap:
                name: commit-audit-oak-repos
